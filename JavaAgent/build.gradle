plugins {
    id("java")
}

compileJava {
    sourceCompatibility = JavaVersion.VERSION_1_8
    targetCompatibility = JavaVersion.VERSION_1_8
}

dependencies {
    compileOnly(project(":Core"))
}

tasks.jar {
    manifest {
        attributes(new HashMap<String, String>() {{
            put("Premain-Class", "dev.lynith.javaagent.AgentMain");
            put("Can-Redefine-Classes", "true");
            put("Can-Retransform-Classes", "true");
        }});
    }
}